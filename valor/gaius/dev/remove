from gaius import RT_CONN
import rethinkdb as r

r.db("transducers").table("galahad").filter({"function": "virtue"}).delete().run(RT_CONN)
r.db("transducers").table("commands").filter({'type':'MIGRATION'}).delete().run(RT_CONN)
print(r.db("transducers").table("galahad").run(RT_CONN))
